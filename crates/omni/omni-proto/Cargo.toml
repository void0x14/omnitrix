[package]
name = "omni-proto"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Canonical shared state model, event and command types for Omnitrix (single source of truth)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true, features = ["serde"] }
thiserror = { workspace = true }

[lints]
workspace = true
