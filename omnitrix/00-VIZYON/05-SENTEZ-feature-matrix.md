# 05 — Sentez: Feature Matrix (15 Alan × 11 Harness)

> Tarih: 2026-08-14 · Girdiler: 01–04 vizyon özetleri + 12 harness RAPOR.md (opencode, codex, crush, aider, grok-cli, pi, oh-my-pi, deepseek-harness, claude-code, cursor-cli, warp, hermes)
> Kullanıcının mutlak kuralları: tek kullanıcı, paylaşım yok · soğuk başlatma 1s bandı (sıcak cache/daemon YASAK) · dil agnostik · ultra bellek verimliliği + 10 bin eşzamanlı ajan vizyonu · sıfır telemetri/egress · anahtarlar RAM'de + zeroize + fail-closed.
> Mevcut varlıklar: grok-build fork'u (xai-*), keychain katmanı, 40+/60+ routing modu, Flow Governor, 9→61 persona.

---

## 1. Feature Matrix

| # | Alan | KAZANAN harness | ÇAL — çalınacak tasarım (somut) | LOBOTOMİ — kusur(lar) | OMNITRIX KARARI |
|---|---|---|---|---|---|
| 1 | **Ajan döngüsü / yürütme** | crush + grok-cli (sampler) | Fold'lu prompt kuyruğu (`drainQueueForStep`: meşgulken gelen promptlar tek step'e birleşir) + deterministik iptal (`cancelMark`/`acceptSeq`) + imza-bazlı loop detection (SHA-256 imza 5×/10 step → zorla durdur). Sampler'ın 3 katmanlı actor tasarımı (client/stream/handle) yeniden yazılamaz kalitede. | opencode: Effect-TS her şeyde + tool yürütmesi AI SDK'ya delege (`providerExecuted` hack'i, çift senkron kaynağı). codex: Responses API'ye gömülü model bağımlılığı. | Ajan döngüsü = **artifact dondurulmuş xai-grok-sampler**; üstüne crush'ın fold + cancel + loop-detection üçlüsü eklenir. Tool döngüsü tek executor'da, SDK sadece transport. |
| 2 | **Context & compaction** | claude-code + pi + opencode | CC'nin **5'li hiyerarşisi**: full auto-compact (eşik %89) + partial (nokta özeti) + sub-agent compact + **microcompact** (LLM'siz, serialization'da eski tool-result'ları `[Tool result cleared]`, son 3 korunur) + cross-session cache. pi'nin **iteratif merge özeti** (initial/update prompt ayrımı, önceki özet `<previous-summary>` olarak merge) + doğru cut-point (geriye yürüme, split-turn prefix). opencode'un **prune ayrı pası** (20k/40k eşikler, compaction'dan ucuz). | pi mid-turn boşluğu (#6339: eşik yalnızca run sınırında kontrol → %100 runaway; reserveTokens sessizce etkisiz). codex 3 ayrı compaction dosyası (2000+ satır). opencode v1/v2 çift yığın (2 ayrı compaction). Compaction KV cache'i öldürür (~$0.40/defa). | Tek modül `omni-compact`: 5'li hiyerarşi + iteratif merge + prune; **eşik her assistant mesajından sonra O(1) guard'la** (pi'nin hatası yapılmaz); **API-raporlu usage esas**, lokal tahmin fallback. Compaction sonrası yeniden enjeksiyon (son-okunan dosyalar + plan + hook sonuçları). |
| 3 | **Kod düzenleme formatı** | oh-my-pi (hashline) + aider | Content-hash anchor'lı edit + **stale-anchor reddi** (ölçülen: ilk-deneme başarısı %6.7→%68.3). aider'in **fuzzy fallback zinciri** (birebir → whitespace toleransı → ellipsis → edit-distance) + **git cherry-pick 3-way merge fallback'i** (original→search→replace commit'leri, search metni sapmışsa bile edit uygulanır) + "Did you mean" geri bildirimi. | sed/awk/cat bash kurnazlıkları (zaten yasak). Gri bölge: modelin satır-yeniden-yazma formatına düşmesi. | Mevcut `grok_build_hashline` compat seti **korunur**; üstüne aider'in cherry-pick 3-way fallback'i + did-you-mean döngüsü eklenir. Edit güvenilirliği = omnitrix'in ölçülebilir kimliği. |
| 4 | **Repo farkındalığı (repo map)** | aider | Tree-sitter tag sorguları (`*-tags.scm`) → identifier→dosya grafı → **personalized pagerank** (chat dosyaları ×50, bahsedilen ident'ler ×10, `_`-prefix ×0.1) → token bütçesine **binary search** ile ağaç snippet (±%15) + mtime önbellek. | crush'ta repo map **YOK** (her seferinde arama). codex/cursor: Repo Merkle hash'ini **satıcı sunucusuna** itme — yapı metadata'sı bile sızar. | Yerel repo map (Rust, xai-codebase-graph üstüne); pagerank + binary-search bütçe. **Hash asla dışarı çıkmaz** — yerel indeksleme yeterli. |
| 5 | **Yetki / güvenlik** | opencode + codex | Wildcard ruleset (`findLast`, default-ask, `always`→session-approved, reject-cascade) + **deny → tool listesinden gizle** (model yasak tool'u denemez, prompt boşa harcanmaz) + doom-loop tespiti (3× aynı tool+input → onay). codex: `*.rules` DSL + BANNED prefix listesi (`bash -lc`, `curl\|sh`, 40+ pattern) + onayın kalıcı kurala dönüşmesi. Kural sırası: defaults → agent → user. | Claude Code: lineer kural taraması + **Unicode confusable bypass'ı** (#29489). crush: yolo/skip mode (hepsi otomatik onay). pi: permission sistemi hiç yok. opencode: reject gerekçesi zayıf. | Tek nokta tool broker (I4). **Precompiled Map + NFKC/confusable normalizasyon** (CC'nin bypass'ı miras alınmaz). Bash deny taxonomy + acil kaçış (K3, yargıç onaylı, loglu). |
| 6 | **Sandbox** | codex (kavram) | Yalnızca **permission profile kavramı** (okuma/yazma/ağ kural kümeleri → policy dönüşümü) alınır; mekanizma alınmaz. | codex 3-platform sandbox (seatbelt/landlock/bwrap + yardımcı binary + MITM CA) = platform/toolchain bağımlılığı. xai-grok-sandbox nono pin'i duruyor ama kullanılmayacak. | **SANDBOX YOK (K5).** İzolasyon = geçici git branch. Codex'ten yalnızca profile/policy fikri; backend atılır. xai-grok-sandbox CI'da ek güvenlik hattı olarak dondurulur, çalışma modeline girmez. |
| 7 | **Kalıcılık / session** | cursor-cli + codex + deepseek | Checkpoint/resume protokolü: **SHA-256 content-addressed blob store** + `resumeAction` + 30s stall tespiti + ≤10 retry — büyük context parçaları ana akıştan ayrı. codex: `ThreadStore` trait'i (append vs metadata ayrımı) + subagent spawn grafı DB'de, restart'ta restore. deepseek: **trajectory** — append-only session log (system prompt, reasoning, tool call, context enjeksiyonu; resume/fork/search/replay aynı stream). | Claude Code `.claude.json`: 3.1 GB tek dosya + tutarsız lock; 1.2 TB disk senaryoları. opencode v1/v2 çift model. Warp: cloud'a devir (bulut bağımlılığı). | Zaten var olan **CAS + WAL crash-only** korunur (I7: önce niyet kaydı, kapanış O(1), açılışta replay); üstüne cursor'ın checkpoint/resume'u + trajectory log. Tek message tablosu: append-only, part'lar JSON kolonda — event sourcing yok. |
| 8 | **Sağlayıcı / model katmanı** | grok-cli (sampler + keychain) | Sampler'ın 3 katmanlı actor'ı + router engine + **401 attribution** + doom-loop kolektörü. keychain: AES-256-GCM + Argon2id (64 MiB) + zeroize + 15 dk RAM TTL + OS keyring. models.dev katalog + 24s TTL cache + `--auto` prefix-detect + canlı probe (2.5s, 200/401 ayrımı, ambiguous hatası). | codex: Responses API'ye gömülü resume/compact (sağlayıcı bağımsızlığını kırar). warp: server-side routing (hangi model neyi gördü bilinmez, GLM 5'e bile gidebilir). cursor: saf röle, offline yok, sürüm tabanı kilidi. | **Artifact dondur**: sampler + keychain + models.dev. 60+ routing modu korunur. BYOK, istemci-tarafı routing — "hangi modele ne gitti" her zaman yerelde kanıtlanabilir. I5: model adı/fiyatı gömülü değil. |
| 9 | **TUI / render** | warp (veri modeli) + crush (render) | **Block modeli**: komut+çıktı ve agent konuşması aynı BlockList'te; SumTree yükseklik indeksi (O(log n) viewport); FlatStorage packed buffer; zero-height gizleme (agent komutları compact summary, istenince expand). crush: streaming markdown **safe-boundary render** (fence/list/HTML/think) + section FNV-hash cache + tema sistemi + diff view. | Claude Code React/Ink: 578 useState, %1.9 memo, streaming'de O(n) reconcile, 200–400 MB baseline. warp: GPU wgpu (Linux'ta Vulkan zorunlu). opencode TUI: 1134 satır app.tsx + 20 dialog. | xai-grok-pager katmanı korunur; blok-bilinçli model render'a işlenir. **10–20 MB sınıfı**; basit buffer/dirty-region — scene graph yok, GPU yok. Block ID ile context referansı (token verimli). |
| 10 | **MCP / araç yaşam döngüsü** | opencode + crush + grok-cli | 5 durumlu status machine (connected/disabled/failed/needs_auth/needs_client_registration) + **ToolListChanged → canlı refresh** + kapanışta `pgrep -P` alt süreç temizliği. crush: **generation-bazlı stale-connect koruması** (teardown sonrası eski connect kaydı atılır) + reconcile. grok-cli: rmcp 2.1 **karantina stratejisi** (`pub use rmcp` ile versiyon çakışması çözümü). | Claude Code MCP proxy (`mcp-proxy.anthropic.com` — üçüncü taraf çağrıda bile method adı + server ID Anthropic'te görünür). deepseek self-modification toolset'i (yüksek risk). OAuth PKCE ilk sürümde ağır. | `xai-grok-mcp` dondurulur; üstüne status machine + generation guard + canlı refresh. **Tünel yok** — MCP trafiği asla kendi sunucundan geçmez. PKCE sonraya; client-credentials ile başla. |
| 11 | **Telemetri / gizlilik** | (anti-desen dersleri: warp, CC, cursor) | Warp'ın doğru parçası: `isTelemetryEnabled` gerçekten kapatıyor + "View network logging" + yayınlanmış telemetry tablosu. CC dersi: tek kanal + tek anahtar olmalıydı. | CC **çift telemetry hattı** (Datadog SDK ayrı kanal: tool adı, permission mode, per-call cost, `file_count:278`). warp: `/analytics/block` (her komut+çıktı+cwd), **HTTP_PROXY bypass + NoKeyLog** (denetim engeli), opt-out restart'ta geri açılıyor. cursor: **ghost mode yalanı** (envelope açık: hostname, OS, RPC span ağacı). | **Varsayılan kapalı, tek anahtar, opt-in.** no-telemetry **patch değil kod silme** (I2a muafiyeti kalkar, I8 gate kalır). Envelope verisi bile yok. Denetim araçlarını engelleyen hiçbir teknik (proxy bypass/NoKeyLog) omnitrix'te bulunamaz. |
| 12 | **Bellek / RAM disiplini** | (ders: CC felaketi + crush DB darboğazı) | crush'ın delta-yazma darboğazından ders: DB'ye **buffer + toplu flush** (her token'da write yok). grok-cli: release'de overflow-checks + debug-assertions, thin LTO, CGU=1, jemalloc. | Claude Code: 11.6 GB/process (#21403), 120 GB+ OOM öncesi (#4953), boşta 200–400 MB. opencode 25 paket. warp GPU zinciri. | RSS < 250 MB ölçülür (omni-bench); bellek sızıntısı = Soak kapısı. Streaming yazımları buffered. `panic="unwind"` daemon + per-agent `catch_unwind` (AS5). İş kutsal (K2): 250 MB hedef darboğaz değil, swap/kuyruk serbest. |
| 13 | **Soğuk başlatma / hız** | pi (minimal) + grok-cli (lazy) | pi: alt-1K token system prompt + minimal ReAct loop. **Lazy skills**: her yetenek kontekste yalnız açıklamasıyla girer, tam şema `/skill:name` ile — baştan şema basımı yok. grok-cli: ilk TUI frame'i sağlayıcısız çizilir, ısınma arka planda; lazy-init entrypoint. | CC: bundle parse %31.7 startup (300ms), 413 senkron FS çağrısı, 1.087 interop wrapper. crush: her run'da provider/tool rebuild (`buildTools` per run) + MCP WaitForInit zorunlu. opencode: Bun startup. | Hedef: **cold < 400ms / sıcak < 100ms** (omni-bench komut+metrik+eşik). Async I/O, tool seti cache'i (crush'ın rebuild'ini alma), lazy skill yükleme, sağlayıcısız ilk frame. |
| 14 | **Test / benchmark disiplini** | aider + deepseek + grok-cli | aider: SWE-bench + polyglot grid (`rungrid.py` model×format) + her release'de koşma kültürü. deepseek: **verify-tool-catalog** — her tool gerçek context'te boot edilip JSON-schema üretir, belgelenmeden eklenemez. grok-cli: I1 kapısı = komut + metrik + eşik; kapı testleri (crash_recovery, diff_visibility, ui_parity, grounding_redteam, tool_allowlist_redteam, provider_fallback, multiagent_fanout, interrupt_granularity). | pi: hiçbir resmi benchmark yok. deepseek: 1 günlük proje — olgunluk kanıtı sıfır. codex: compaction 3 ayrı dosyada test edilemez halde. | I1 invariant'ı: her faz kapısı = komut+metrik+eşik, CI'da bloklayıcı. Edit formatı + compaction için kendi polyglot-grid'i; tool kataloğu doğrulama hattı. Derleyici yasağı dönemi bitti — ölçüm her zaman gerçek. |
| 15 | **Çoklu-ajan / orkestrasyon** | deepseek + oh-my-pi + codex | deepseek: continuable background subagent + `send_message`/`interrupt_agent`/`list_agents` kontrol tool'ları + `job_kill/job_list/job_output` birleşik job kontrolü. oh-my-pi: task fan-out — izole worktree'lerde paralel subagent'lar + **schema-doğrulamalı typed sonuç**. codex: subagent spawn grafı DB'de (ThreadSpawnEdgeStatus), restart'ta restore. | opencode: background subagent env flag'ine gömülü (kararsız). warp: orkestrasyon cloud'da (laptop kapanınca "cloud'a devir" = bağımlılık). CC: agent teams crash'leri. | **OmniSchedulerBackend** (admission/kuyruk/depth — zaten yazıldı) + tier modeli (Existing→Sleeping→Queued→Active) korunur; deepseek'in kontrol tool seti + typed sonuç eklenir. Derinlik tavanı 5, fan-out tavanı 8, bütçe ebeveyn zarfı. Çalışan görev asla düşürülmez (K2). |
| 16 | **Bellek & öğrenme (HERMES EKLENTİSİ)** | hermes + codex | hermes: **frozen-snapshot memory** — MEMORY.md/USER.md session başında system prompt'a donmuş enjekte edilir; session içi yazım diske anında (fcntl lock + atomik) ama prompt'a dokunmaz → prefix cache korunur; nudge sayacı (her N turn). kapalı öğrenme döngüsü: skill-creation nudge (her N tool iterasyonu) + turn-sonrası arka plan review + curator konsolidasyonu. codex: 2 fazlı session-sonrası bellek pipeline'ı. | hermes: ~30K token/event review fork maliyeti; 2200/1375 char dar limit; curator yanlış-silme riski. codex: her oturum LLM çağrısı. | `omni-memory`: frozen-snapshot deseni (cache-dostu, ucuz) + nudge; review fork'u AYRI ajan değil düşük maliyetli modelle kendi tool'ları üzerinden; konsolidasyon gece/çapraz-oturum işi. |

----

### HERMES notları (matrix sonrası eklenen 12. harness)

-- **Kalıcılık alanına (satır 7) ek:** hermes memory yazımı atomik + dosya-kilitli (SQLite şart değil); omnitrix WAL ile uyumlu.
-- **Test/benchmark alanına (satır 14) ek:** hermes'in GEPA/DSPy self-evolution hattı (ayrı repo, ICLR 2026 Oral) skill prompt'larını evrimleştirir — omnitrix persona prompt'ları için ileride değerlendirilir.
-- **Soğuk başlatma alanına (satır 13) ek:** frozen-snapshot memory ek startup maliyeti getirmez (diskten tek okuma, prompt'a statik ekleme).

---

## 2. Kesişen sentez kararları (tüm alanları bağlayan)

1. **Dil politikası:** Rust çekirdek (xai-* zorunluluğu, tokio çok-ajan, jemalloc, tek binary, ms soğuk başlatma) + **Zig** bağımsız native'ler (hashline/walker/scan/bench — sıfır runtime, C ABI, kullanıcı tercihi). Go/Python runtime'a girmez; yalnızca araç üretimi (bench analiz). "Her parça en iyi dilde" kuralı tek binary vizyonuyla dengelenir: N runtime = N bakım yükü.
2. **Orkestrasyon ≠ ajan yazımı:** ajan runtime, LLM taşıma, tool sistemi vendored xai-*'tan gelir (I2). Omnitrix üstüne planlama, yönlendirme, persona, besleme, dayanıklılık ekler. "İnşa et, fork etme."
3. **Determinizm süreçte (D2):** akış AI inisiyatifinde değildir — Flow Governor durum makineleri karar verir, AI sonuçları üretir. Şablonlar/zarf/kapı sisteme aittir.
4. **İş kutsal (K2):** 250 MB hedef, darboğazda swap-out/kuyruklama ile esnetilir; iş asla yarıda kesilmez. "Bitmiş işe kaynak yakmak yasak" (K11).
5. **Güvenilirlik protokol seviyesinde:** stall/heartbeat, checkpoint/resume, WAL niyet kaydı — "umut" değil mekanizma (CC'nin ölü watchdog'undan ders).

## 3. En çarpıcı 6 karar (özet)

1. **Compaction, tek modül:** CC 5'li hiyerarşi (microcompact dahil) + pi iteratif merge + opencode prune; mid-turn boşluğu O(1) guard'la kapatılır — rakiplerin hiçbirinde 5'i birden yok.
2. **Permission, opencode ruleset + codex execpolicy:** deny → tool listesinden gizleme + doom-loop + BANNED prefix + NFKC normalizasyon — pi'ın "permission yok" ve CC'nin "Unicode bypass" uçlarına düşülmez.
3. **Telemetri = kod silme:** patch değil; CC'nin Datadog çift hattı, warp'ın proxy bypass'ı, cursor'un ghost-mode yalanı hiçbir biçimde kopyalanmaz; tek anahtar, varsayılan kapalı.
4. **Sandbox yok (K5):** codex'ten yalnızca permission-profile kavramı; izolasyon geçici git branch.
5. **TUI = blok modeli:** warp'ın BlockList/SumTree veri modeli pager'a işlenir; React/Ink (200 MB) ve GPU rendering (Vulkan) kategorik olarak yasak.
6. **Dil:** Rust çekirdek + Zig native'ler; Go/Python araç-üretimi dışında tutulur — 10 bin ajan tek binary'den yönetilir.

*Kaynaklar: 01-master-plan-ozet, 02-docs-specs-ozet, 03-git-feature-envanteri, 04-config-persona-envanteri + 11 RAPOR.md (opencode/codex/crush/aider/grok-cli/pi/oh-my-pi/deepseek-harness/claude-code/cursor-cli/warp).*
